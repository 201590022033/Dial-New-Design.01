import { describe, expect, it } from 'vitest';
import { PARAMETRIC_CASE_V1, PARAMETRIC_HAND_SET_V1, PARAMETRIC_HAND_V1, validateParametricCaseV1, validateParametricHandV1, type ParametricCaseV1, type ParametricHandV1, type ParametricHandSetV1 } from '@/domain/geometry/parametric';

const u = { status: 'unknown' as const };
const baseCase = (): ParametricCaseV1 => ({ schema: PARAMETRIC_CASE_V1, ...Object.fromEntries(['caseDiameter','midcaseHeight','lugWidth','lugToLug','dialOpening','crystalSeatDiameter','casebackOpening','upperCaseRadiusReduction','lowerCaseRadiusReduction','middleCaseBulge','bezelLipHeight','casebackLipHeight','lugRootWidth','lugTipWidth','lugPairGap','lugCaseOverlap','lugTipDrop','lugThickness','lugTaperStrength','crownTubeRadius','crownTubeLength','crownBossRadius','crownBossLength','crownBossEmbed','crownTubeEmbed'].map(k => [k, 1])), caseDiameter: 40, lugToLug: 42, lugWidth: 6, lugTipWidth: 5, dialOpening: 30, crystalSeatDiameter: 32 } as ParametricCaseV1);
const hand = (): ParametricHandV1 => ({ schema: PARAMETRIC_HAND_V1, hub: { diameter: 2, thickness: 0.2, pinionHoleDiameter: 0.5 }, body: { length: 10, rootWidth: 1, distalWidth: 0.5, thickness: 0.1 }, tip: { style: 'POINT', length: 2, width: 0.5, shoulderSweep: 0, lume: true }, tail: { style: 'NONE', length: 0, width: 0, lume: false }, lume: { body: true, tip: false, tail: false } });
describe('parametric contracts', () => { it('exposes versions and validates known case/hand', () => { expect(PARAMETRIC_HAND_SET_V1).toBe('parametric-hand-set/v1'); expect(validateParametricCaseV1(baseCase()).status).toBe('valid'); expect(validateParametricHandV1(hand()).status).toBe('valid'); }); it('distinguishes invalid from unknown', () => { const c = baseCase(); c.caseDiameter = 0; expect(validateParametricCaseV1(c).status).toBe('invalid'); const h = hand(); h.hub.pinionHoleDiameter = u; expect(validateParametricHandV1(h).status).toBe('unknown'); }); it('supports variants, independent lume, and optional seconds', () => { const h = hand(); h.tip.style = 'TRIANGLE'; h.tail.style = 'COUNTERWEIGHT'; expect(h.lume).toEqual({ body: true, tip: false, tail: false }); const set: ParametricHandSetV1 = { schema: PARAMETRIC_HAND_SET_V1, hour: h, minute: h }; expect(set.seconds).toBeUndefined(); }); });
