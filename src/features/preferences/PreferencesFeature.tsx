import { FeaturePlaceholder } from '@/features/shared/FeaturePlaceholder';

export const PreferencesFeature = () => {
  return (
    <FeaturePlaceholder
      title="Preferences"
      description="Workspace and rendering preferences placeholder for snapping behavior, guide styles and UI ergonomics."
    />
  );
};
