import { FeaturePlaceholder } from '@/features/shared/FeaturePlaceholder';

export const ExportFeature = () => {
  return (
    <FeaturePlaceholder
      title="Export"
      description="Export pipeline extension point for SVG, PNG and print-oriented outputs with guide and bleed options."
    />
  );
};
